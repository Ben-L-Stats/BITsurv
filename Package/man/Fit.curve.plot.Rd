% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0. Fit curves and plot.R
\name{Fit.curve.plot}
\alias{Fit.curve.plot}
\title{Fits a parametric survival curve to data and plots the results}
\usage{
Fit.curve.plot(surv.data, Distribution, break.time = 10)
}
\arguments{
\item{surv.data}{A dataframe with 2 columns labelled time and event.}

\item{Distribution}{Pick your parametric survival distribution. Available options are:
'exp', 'weibull', 'gompertz', 'llogis','lnorm', 'gamma', and 'gengamma'. See
flexsurv for further details on these distributions.}

\item{break.time}{Gives the labelled breaks in time on the survival plot and number and risk plot.}
}
\value{
A plot: KM curve (in purple), an overlaid  fitted parametric curve (in black), and
number at risk.
}
\description{
This function produces a plot
}
\examples{
#The dplyr package is used to make the simple.data
simple.data<-data.frame(T=rexp(100, rate=1/10),        #the underlying event process
                        cens=runif(100, 0, 20)) \%>\%    #the underlying censor process
     mutate(event=ifelse(T<=cens,0, 1),
            time=ifelse(T<=cens,T,cens)) \%>\%
     select(time,event)

#Produce the plot
Fit.curve.plot(surv.data=simple.data,
               Distribution='exp',
               break.time=5)

}
